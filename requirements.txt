# Core dependencies - let pip resolve compatible versions
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0
httpx>=0.24.0

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# For compatibility with our schemas
typing-extensions>=4.7.0
